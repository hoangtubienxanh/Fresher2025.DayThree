Objective:
Implement an abstract class Car, create concrete classes FuelCar and ElectricCar, and use DateTime for
tracking maintenance and fueling/charging history.

Endpoints:

POST /car
PUT /car/{id}
GET /car/{id}
DELETE /car/{id}

POST car/{id}/do.recharge